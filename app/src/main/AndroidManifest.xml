<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.imhanjie.v2ex">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/BasicAppTheme">

        <activity android:name=".view.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".view.TopicActivity" />

        <activity
            android:name=".view.LoginActivity"
            android:launchMode="singleTask" />

        <activity android:name=".view.NodeActivity" />

        <activity android:name=".view.FavoriteTopicsActivity" />

        <activity android:name=".view.MemberActivity" />

        <activity
            android:name=".view.CreateTopicActivity"
            android:theme="@style/BasicAppTheme.WhiteWindow"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".view.SearchNodeActivity"
            android:theme="@style/BasicAppTheme.WhiteWindow" />

        <activity android:name=".view.PreviewTopicActivity" />

        <activity
            android:name=".view.AppendTopicActivity"
            android:theme="@style/BasicAppTheme.WhiteWindow"
            android:windowSoftInputMode="adjustResize" />

    </application>

</manifest>